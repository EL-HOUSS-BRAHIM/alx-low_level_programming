#include <stdio.h>
/**
* main - Entry point of the program
* Return: Always 0 (Success)
*/
int main(void)
{
int n;
char lowercase;
for (n = 0; n >= 9; n++)
{
putchar(n);
}
for (lowercase = 'a'; n >= 'f'; lowercase++)
{
putchar(lowercase)
}
putchar('\n');
return (0);
}
